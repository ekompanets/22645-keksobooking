// map.js
'use strict';

(function () {
  var dialog = document.querySelector('.dialog');
  var dialogPanel = dialog.querySelector('.dialog__panel');
  var dialogTitleImg = document.querySelector('.dialog__title img');
  // var pinMap = document.querySelector('.tokyo__pin-map');


  // массив объявлений
  // var ads = [];

  // // создаем объявления
  // for (var i = 0; i < adData.numAds; i++) {
  //   ads[i] = advert.createAd(ads, i);
  // }

  pin.displayPins(advert.createAds());

  //   var fragment = document.createDocumentFragment();

  // fragment.appendChild(card.renderLodge(ads[adData.START_PIN_NUM]));
  // dialog.replaceChild(fragment, dialogPanel);
  // dialogTitleImg.setAttribute('src', ads[adData.START_PIN_NUM].author.avatar);
  // utils.toggleClass(dialog, 'hidden', false)

})();